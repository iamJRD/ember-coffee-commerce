<div class="row">
  {{#each model as |item|}}
    <div class="col-md-4">
      {{item.brand}}
      {{#link-to 'item' item.id}}{{item.name}}{{/link-to}}
      {{item.quantity}}
      {{#link-to 'item' item.id}}<img src={{item.image}} />{{/link-to}}
    </div>
  {{/each}}
</div>

{{display-cart}}
